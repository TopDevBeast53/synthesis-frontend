import { getHelixLPSwapAddress } from 'utils/addressHelpers'

export const lpSwapAddress = getHelixLPSwapAddress()

export const ToolTipText = (lpTokenSymbol, lpTokenAmount, isGivingTokenLp, exTokenSymbol, exTokenAmount, isAskingTokenLp, isLiquidity = false) =>
    !isLiquidity ? `The swap creator is offering ${isGivingTokenLp ? "the yield generated by" : ""} ${lpTokenAmount} ${lpTokenSymbol} ${isGivingTokenLp ? "LP" : ""} token and is asking for ${isAskingTokenLp ? "the yield generated by" : ""} ${exTokenAmount} ${exTokenSymbol} ${isAskingTokenLp ? "LP" : ""} tokens.`
        : `The swap creator is offering ${lpTokenAmount} ${lpTokenSymbol} ${isGivingTokenLp ? "LP" : ""} token and is asking for ${exTokenAmount} ${exTokenSymbol} ${isAskingTokenLp ? "LP" : ""} tokens.`